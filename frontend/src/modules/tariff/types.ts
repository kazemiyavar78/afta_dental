// این ماژول نمونه است؛ باقی فیلدها/صفحات طبق همین الگو در فازهای بعدی اضافه می‌شوند.

export type Tariff = { id: number; name: string; amount: number };
export type CreateTariffPayload = { name: string; amount: number };
