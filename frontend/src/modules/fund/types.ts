// این ماژول نمونه است؛ باقی فیلدها/صفحات طبق همین الگو در فازهای بعدی اضافه می‌شوند.

export type Fund = { id: number; name: string; balance: number };
export type CreateFundPayload = { name: string };
